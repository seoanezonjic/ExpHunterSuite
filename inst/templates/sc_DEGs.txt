
<!--begin.rcode setup
    comparison <- plotter$hash_vars$target_name
    opt <- plotter$hash_vars$opt
    query <- plotter$hash_vars$query
    DEG_list <- plotter$hash_vars$DEG_list
    comparison_meta <- DEG_list$DEGs$meta
    DEG_legend <- paste0("<p>", comparison_meta$high, " (positive log2FC) vs ",
                         comparison_meta$low, " (negative log2FC). </p>")
    DEG_metrics_list <- plotter$hash_vars$DEG_metrics_list
    DEG_query_list <- plotter$hash_vars$DEG_query_list
    comparison_metrics <- DEG_list$DEG_metrics
    query_metrics <- DEG_list$DEG_query
    plotter$hash_vars$comparison_FCs <- comparison_metrics$DEG_df
    plotter$hash_vars$comparison_ncells <- comparison_metrics$ncell_df
    comparison_DEGs <- DEG_list$DEGs
    plotter$hash_vars$FC_distribution <- DEG_list$FC_distribution

    DEGs_volcano <- function(input_DEGs) {
       alphas <- apply(input_DEGs[, c("pct.1","pct.2")], 1, function(x) return(max(x)))
       EnhancedVolcano::EnhancedVolcano(input_DEGs, lab = rownames(input_DEGs), title = DEG_level, x = 'avg_log2FC',
                        y = 'p_val_adj', ylim = -0.5,  pCutoff = opt$p_val_cutoff, FCcutoff = opt$min_avg_log2FC,
                        colAlpha = alphas, legendLabels = c("Discarded", expression("Log"[2]*"FC"), "adj P-val", "Significant"))
    }

    get_DEG_table <- function(input_DEGs) {
        res <- input_DEGs[, c("gene", "p_val", "p_val_adj", "avg_log2FC", "pct.1", "pct.2", "filter", "prevalent")]
        if(any(dim(res) < 1)) {
            res <- NULL
        }
        return(res)
    }
end.rcode-->

<h1><strong>SINGLE CELL RNA-SEQ DIFFERENTIAL EXPRESSION ANALYSIS</strong></h1>

<!--begin.rcode sample_tables, echo = FALSE, warning=FALSE
    cat(plotter$create_title("<b>DESCRIPTION OF SAMPLES</b>", id = "samples_title", hlevel = 2, indexable =TRUE))
    cat(plotter$create_title("First group of samples (to be referred to as control in the rest of the report)",
                              id = "control_table", hlevel = 3, indexable = FALSE))
    plotter$hash_vars$controls <- plotter$hash_vars$target$sample[plotter$hash_vars$target$treat == "Ctrl"]
    cat(make_html_list(plotter$hash_vars$controls))
    cat(plotter$create_title("Second group of samples (to be referred to as treatment in the rest of the report)",
                              id = "treatment_table", hlevel = 3, indexable = FALSE))
    plotter$hash_vars$treat <- plotter$hash_vars$target$sample[plotter$hash_vars$target$treat == "Treat"]
    cat(make_html_list(plotter$hash_vars$treat))
end.rcode-->

<!--begin.rcode GLOBAL_DEGs
    cat(plotter$create_title(paste0("<b>GLOBAL DEG ANALYSIS: TARGET ", toupper(comparison), "</b>"), paste0(comparison, "_DEGs"), hlevel = 2,
                         indexable = TRUE, clickable = FALSE, t_id = paste0(comparison, "_DEGs"),
                         clickable_text = ""))
    legend <- "<p><b>DEG ANALYSIS GLOSSARY:</b></p>
             <ul>
                 <li> <b>GLOBAL</b>: DEGs calculated on all cells in the experiment. Cells are divided by the category selected for analysis, and DEGs are calculated
                 between the two groups. </li>
                 <li> <b>Cell group</b>: DEGs calculated on cells belonging to the same putative cell type. </li>
                 <ul>
                     <li> First, the subset of cells belonging to the specified cell group are subset from the experiment. </li>
                     <li> Then, it is divided in two groups for DEG calculation. </li>
                 </ul>
             </ul>"
    cat(legend)
    DEG_level <- "GLOBAL"
    title <- "Global"
    names(comparison_DEGs$markers)[names(comparison_DEGs$markers) == "global"] <- "GLOBAL"
    plotter$hash_vars$comparison_DEGs <- comparison_DEGs$markers[["GLOBAL"]]
    DEG_legend <- paste0("<p>", comparison_DEGs$meta$high, " (positive log2FC) vs ",
                         comparison_DEGs$meta$low, " (negative log2FC). </p>")
    cat(DEG_legend)
    if(is.null(plotter$hash_vars$comparison_DEGs)) {
        cat(paste("<p> No significant DEGs found for cell group", DEG_level, "</p>"))
    } else if(nrow(plotter$hash_vars$comparison_DEGs) < 1 | isFALSE(unlist(plotter$hash_vars$comparison_DEGs))) {
        cat(paste("<p> No significant DEGs found for cell group", DEG_level, "</p>"))
    } else {
        cat(plotter$prettify_div(plotter$static_plot_main("comparison_DEGs", custom_format = TRUE,
            plotting_function = DEGs_volcano, resizable = TRUE,), preset = "magic"))
        cat(plotter$prettify_div(plotter$table(list(id = "comparison_DEGs", header = NULL, row_names = NULL,
                                                    table_rownames = FALSE, styled = "dt", text = "dynamic",
                                                    func = get_DEG_table)), preset = "magic"))
    }
end.rcode-->

<!--begin.rcode DEG_summary, eval = length(DEG_list) > 0
    cat(plotter$create_title(paste0("<b>DIFFERENTIAL EXPRESSION SUMMARY OF CELL GROUPS: TARGET ", toupper(comparison), "</b>"),
        "DEG_metrics", hlevel = 2, indexable = TRUE, clickable = FALSE, t_id = "DEG_metrics", clickable_text = ""))
    cat("<p>These plots show an overview of the most significant DEGs across the entire dataset. They extract the top DEGs
         from each individual cell group comparison, and then generate the union of these lists. This union is then used
         in the plots shown below, representing FC values of these genes for every cell group DEG analysis.</p>")
    cat(paste0("<p>A minimum feature count filter has been applied in this step, which means DEGs considered",
               " for this plot follow stricter criteria. This was done in order to not overload the figure.</p>"))
    if(any(dim(plotter$hash_vars$comparison_FCs) < 1) | is.null(plotter$hash_vars$comparison_FCs)) {
        cat(paste0("<p> No significant DEGs found for target ", comparison, ".</p>"))
    } else {
        DEG_legend <- paste0("<p>", comparison_meta$high, " (positive log2FC) vs ",
        comparison_meta$low, " (negative log2FC). </p>")
        cat(DEG_legend)
        fc <- plotter$heatmap(list(id = "comparison_FCs", header = NULL, row_names = NULL, text = "dynamic",
                    x_label = "log2FC", title = "Gene average log2FC", transpose = FALSE))
        fc_vs_ncells <- plotter$heatmap(list(id = "comparison_FCs", header = NULL, row_names = NULL, text = "dynamic",
                    extra_data = list(id ="comparison_ncells", header = NULL, row_names = NULL, text = "dynamic", transpose = FALSE),
                    config = list(sizeLegendTitle = "Number of cells"), x_label = "log2FC",
                    title = "Gene average log2FC vs cells expressing gene", transpose = FALSE))
        grid_code <- paste(list(fc, fc_vs_ncells), collapse = "\n\n")
        cat(plotter$prettify_div(plotter$prettify_div(grid_code, width = "1200px"), overflow = "hidden"))
    }
end.rcode-->

<!--begin.rcode DEG_analysis
    no_global_degs <- comparison_DEGs$markers[names(comparison_DEGs$markers) != "GLOBAL"]
    if(length(no_global_degs) < 1) {
        cat("<p> No significant DEGs found in per-cell-type analysis </p>")
    } else {
        cat(plotter$create_title(paste0("<b>CELL GROUP DEG ANALYSIS: TARGET ", toupper(comparison), "</b>"), paste0(comparison, " DEG identities"),
                             hlevel = 2, indexable = FALSE, clickable = FALSE))
        for (j in seq(length(no_global_degs))) {
            DEG_level <- names(no_global_degs)[j]
            title <- paste0("Cell group ", DEG_level)
            plotter$hash_vars$comparison_DEGs <- no_global_degs[[j]]
            if(is.null(plotter$hash_vars$comparison_DEGs)) {
                main <- plotter$prettify_div(paste0("<p>Cell type ", DEG_level, " contained no significant DEGs.</p>"), preset = "magic")
            } else if(FALSE %in% no_global_degs[[j]]) {
                main <- plotter$prettify_div(paste0("<p>Cell type ", DEG_level, " contained exclusive pairs,
                                         DEG analysis was not possible.</p>"), preset = "magic")
            } else {
                main <- plotter$prettify_div(plotter$static_plot_main("comparison_DEGs", custom_format = TRUE,
                                 plotting_function = DEGs_volcano, resizable = TRUE,), preset = "magic")
                table_code <- plotter$prettify_div(plotter$table(list(id = "comparison_DEGs", header = NULL,
                                                                 row_names = NULL, table_rownames = FALSE,
                                                                 styled = "dt", text = "dynamic", func = get_DEG_table)),
                                                   preset = "magic")
                ident_content <- paste(DEG_legend, main, table_code, collapse = "\n")
                cat(plotter$create_title(id = paste0(DEG_level, " DEG results"), paste0(DEG_level, "_DEG_results"), hlevel = 3, indexable = FALSE,
                clickable = TRUE, t_id = paste0(DEG_level, "_collapsable"), clickable_text = ""))
                cat(plotter$create_collapsable_container(id = paste0(DEG_level, "_collapsable"), html_code = ident_content))
            }
        }
    }
end.rcode-->

<!--begin.rcode DEG_query, eval = length(DEG_list) > 0
    if(is.null(query_metrics)) {
        cat(paste0("<p>Query DEG analysis was not possible for analysis ", comparison, ".<p>"))
    } else {
        plotter$hash_vars$comparison_FCs <- query_metrics$DEG_df
        plotter$hash_vars$comparison_ncells <- query_metrics$ncell_df
        cat(plotter$create_title(paste0("<b>DEG QUERY SUMMARY FOR TARGET ", toupper(comparison), "</b>"), paste0(comparison, "_DEG_query"), hlevel = 2,
                     indexable = TRUE, clickable = FALSE, t_id = paste0(comparison, "_DEG_query"), clickable_text = ""))
        if(any(dim(plotter$hash_vars$comparison_FCs) < 1)) {
            cat(paste0("<p> No significant DEGs found for target ", comparison, ".</p>"))
        } else {
            cat(DEG_legend)
            fc <- plotter$heatmap(list(id = "comparison_FCs", header = NULL, row_names = NULL, text = "dynamic",
                                  x_label = "log2FC", transpose = FALSE, title = "Gene average log2FC vs cells expressing gene"))
            fc_vs_ncells <- plotter$heatmap(list(id = "comparison_FCs", header = NULL, row_names = NULL, text = "dynamic",
                    extra_data = list(id ="comparison_ncells", header = NULL, row_names = NULL, text = "dynamic", transpose = FALSE),
                    config = list(sizeLegendTitle = "Number of cells"), x_label = "log2FC",
                    title = "Gene average log2FC vs cells expressing gene", transpose = FALSE))
            grid_code <- paste(list(fc, fc_vs_ncells), collapse = "\n\n")
            cat(plotter$prettify_div(plotter$prettify_div(grid_code, width = "1200px"), overflow = "hidden"))
        }
    }
        
end.rcode-->

<!--begin.rcode params
    cat(plotter$create_title("<b>EXECUTION PARAMETERS</b>", "params", hlevel = 1, indexable = TRUE,
        clickable = FALSE, t_id = "params", clickable_text = ""))
    opt_names <- names(plotter$hash_vars$params)
    opt_values <- paste(plotter$hash_vars$params)
    opt_vector <- paste(opt_names, opt_values, sep = ": ")
    cat(make_html_list(opt_vector))
end.rcode-->

